export default class Documento {
    constructor(
        public id: string,
        public tipo: string,
        public url: string,
        public entregue: string,
        public ServerRelativeUrl: string
    ){

    }
}